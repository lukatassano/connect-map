import { ZoomControl } from "react-leaflet";
import { MapBox } from "./map-box";
import { ClusterMarker } from "./map-cluster-marker";
import { LoadBar } from "./map-load-bar";
import { Marker } from "./map-marker";
import { Tooltip } from "./map-tooltip";

export const Maps = {
	MapBox,
	LoadBar,
	ZoomControl,
	Marker,
	ClusterMarker,
	Tooltip,
};
